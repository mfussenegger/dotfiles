---
- hosts: localhost
  gather_facts: true
  roles:
    - stow
    - aur
  tasks:
    - name: Install dev packages
      become: true
      package:
        name:
          - jq
          - ripgrep
          - bash-language-server
          - hub
          - luarocks
          - ninja
